# Photo folders

Put final presentation photos here if you want local copies instead of Google Drive thumbnails.

Suggested structure:

- `germany/`
- `switzerland/`
- `italy/`
- `austria/`

Then update the `sharedPhotos` / chapter photo data in `script.js` with local paths such as `assets/photos/germany/arrival.jpg`.
